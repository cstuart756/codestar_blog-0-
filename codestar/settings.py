"""
Django settings for codestar project.

Generated by 'django-admin startproject'.
"""

from pathlib import Path
import os

import dj_database_url

# BASE_DIR points to the project root
BASE_DIR = Path(__file__).resolve().parent.parent

# Build paths for global templates and static directories
TEMPLATES_DIR = BASE_DIR / "templates"

# Load env.py if present (local development only).
# env.py is not committed to GitHub and will not exist on Heroku.
if (BASE_DIR / "env.py").is_file():
    import env  # noqa: F401


# SECURITY WARNING: keep the secret key used in production secret!
# Must be set in env.py locally and in Heroku Config Vars in production.
SECRET_KEY = os.environ.get("SECRET_KEY")
if not SECRET_KEY:
    raise ValueError("Missing SECRET_KEY environment variable")

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

# Hosts
# Local development can safely use the defaults.
# For Heroku, set ALLOWED_HOSTS in Config Vars (comma-separated), e.g.:
# ALLOWED_HOSTS=your-app-name.herokuapp.com
if DEBUG:
    ALLOWED_HOSTS = ["127.0.0.1", "localhost"]
else:
    allowed_hosts_env = os.environ.get("ALLOWED_HOSTS", "")
    ALLOWED_HOSTS = [h.strip() for h in allowed_hosts_env.split(",") if h.strip()]

    # If you don't set ALLOWED_HOSTS on Heroku, Django will block requests.
    if not ALLOWED_HOSTS:
        raise ValueError("Missing ALLOWED_HOSTS environment variable in production")


# Application definition

INSTALLED_APPS = [
    # Default Django apps
    "django.contrib.admin",
    "django.contrib.auth",
    "django.contrib.contenttypes",
    "django.contrib.sessions",
    "django.contrib.messages",
    "django.contrib.staticfiles",

    # Third-party
    "django_summernote",

    # Your apps
    "blog",
]

MIDDLEWARE = [
    "django.middleware.security.SecurityMiddleware",
    "whitenoise.middleware.WhiteNoiseMiddleware",
    "django.contrib.sessions.middleware.SessionMiddleware",
    "django.middleware.common.CommonMiddleware",
    "django.middleware.csrf.CsrfViewMiddleware",
    "django.contrib.auth.middleware.AuthenticationMiddleware",
    "django.contrib.messages.middleware.MessageMiddleware",
    "django.middleware.clickjacking.XFrameOptionsMiddleware",
]

ROOT_URLCONF = "codestar.urls"

TEMPLATES = [
    {
        "BACKEND": "django.template.backends.django.DjangoTemplates",
        # Global templates directory (top-level /templates)
        "DIRS": [TEMPLATES_DIR],
        "APP_DIRS": True,
        "OPTIONS": {
            "context_processors": [
                "django.template.context_processors.debug",
                "django.template.context_processors.request",
                "django.contrib.auth.context_processors.auth",
                "django.contrib.messages.context_processors.messages",
            ],
        },
    }
]

WSGI_APPLICATION = "codestar.wsgi.application"
ASGI_APPLICATION = "codestar.asgi.application"


# Database
# Preferred: DATABASE_URL (PostgreSQL) via env var (env.py locally / Config Vars on Heroku)
database_url = os.environ.get("DATABASE_URL")

if database_url:
    # NOTE: ssl_require=True is appropriate for Heroku Postgres.
    DATABASES = {
        "default": dj_database_url.parse(
            database_url,
            conn_max_age=600,
            ssl_require=True,
        )
    }
else:
    # Fallback for local dev if DATABASE_URL is not set.
    DATABASES = {
        "default": {
            "ENGINE": "django.db.backends.sqlite3",
            "NAME": BASE_DIR / "db.sqlite3",
        }
    }


# Password validation

AUTH_PASSWORD_VALIDATORS = [
    {"NAME": "django.contrib.auth.password_validation.UserAttributeSimilarityValidator"},
    {"NAME": "django.contrib.auth.password_validation.MinimumLengthValidator"},
    {"NAME": "django.contrib.auth.password_validation.CommonPasswordValidator"},
    {"NAME": "django.contrib.auth.password_validation.NumericPasswordValidator"},
]


# Internationalization

LANGUAGE_CODE = "en-us"
TIME_ZONE = "UTC"
USE_I18N = True
USE_TZ = True


# Static files (CSS, JavaScript, Images)

STATIC_URL = "/static/"

# Where Django will ALSO look for static files (top-level /static)
STATICFILES_DIRS = [BASE_DIR / "static"]

# Where collectstatic will put files for production
STATIC_ROOT = os.path.join(BASE_DIR, "staticfiles")

STATICFILES_STORAGE = "whitenoise.storage.CompressedManifestStaticFilesStorage"


# Default primary key field type

DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"


# CSRF Trusted Origins (needed for CI IDE / Heroku in many setups)

CSRF_TRUSTED_ORIGINS = [
    "https://*.codeinstitute-ide.net",
    "https://*.herokuapp.com",
]
